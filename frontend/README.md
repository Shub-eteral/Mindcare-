# Frontend - MindCare AI

## Setup
```bash
cd frontend
npm install
cp .env.example .env
npm run dev
```

Runs at http://localhost:5173. Make sure the backend is running first
(see ../backend/README.md) or the chat window will fail to load a session.
